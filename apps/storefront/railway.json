{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm ci && npm run build --filter=@dtc/storefront"
  },
  "deploy": {
    "startCommand": "cd apps/storefront && next start -p 8000"
  }
}
