{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm ci --workspace=storefront && npm run build --workspace=storefront"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=storefront"
  }
}
