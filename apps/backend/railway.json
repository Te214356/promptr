{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm ci --workspace=@dtc/backend && npm run build --workspace=@dtc/backend"
  },
  "deploy": {
    "startCommand": "ln -sfn /mnt/uploads apps/backend/.medusa/server/static && cd apps/backend/.medusa/server && medusa start"
  }
}
